# Rebind shortcuts: group -> action -> key (string, list, or [] to unbind).
[icedos.desktop.kde.shortcuts.bindings]

# Command hotkeys — arbitrary shell on keypress; bind only commands you trust.
# Shape: name -> { command, key = "..." (or keys = [...]), comment = "..." }.
[icedos.desktop.kde.shortcuts.hotkeys]
